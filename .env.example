# Environment Configuration for StudyStreak

# ===================
# Supabase Settings
# ===================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
SUPABASE_STORAGE_BUCKET=learning-materials-v2

# ===================
# AI Service Settings
# ===================
AI_SERVICE_URL=http://localhost:8000
# Optional: API key for AI service authentication
AI_SERVICE_API_KEY=

# AI Service Host/Port (for ai-service container only)
AI_SERVICE_HOST=0.0.0.0
AI_SERVICE_PORT=8000

# Model cache directory (for faster model loading)
MODEL_CACHE_DIR=./models

# ===================
# API Settings
# ===================
# Comma-separated list of allowed CORS origins
API_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000


# ===================
# Logging
# ===================
LOG_LEVEL=INFO

# ===================
# Development Settings
# ===================
# Set to 'true' in development
DEBUG=false

# ===================
# GPU / CUDA Settings (optional)
# ===================
# Control whether the AI service should attempt to use the GPU.
# Options: auto | cpu | cuda | 0 | 1 | true | false
# - auto: use GPU if available, otherwise fall back to CPU
# - cpu: force CPU-only
# - cuda or true or 1: prefer GPU (will fall back if unavailable)
USE_GPU=auto

# Which CUDA device index to use (0-based). Only relevant when USE_GPU enables CUDA.
CUDA_DEVICE=0

# Optional: for Windows users with Tesseract/Poppler installed, point to the executables
# TESSERACT_CMD may be the full path to tesseract.exe or the folder containing it
# POPPLER_PATH should point to the folder that contains pdftoppm.exe (or a parent folder; service will try to normalize)
TESSERACT_CMD=
POPPLER_PATH=
